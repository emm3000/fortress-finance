# Entorno de testing (backend)
# Copia este archivo a .env.test y ajusta valores según tu entorno.
# cp .env.test.example .env.test

DATABASE_URL="postgresql://<DB_USER>:<DB_PASSWORD>@localhost:5433/jaa_finanzas_test"
TEST_DATABASE_URL="postgresql://<DB_USER>:<DB_PASSWORD>@localhost:5433/jaa_finanzas_test"
JWT_SECRET="CHANGE_ME_to_a_random_string_of_at_least_32_characters"
PORT=4000
NODE_ENV=test

# Evitar side effects durante tests locales
ENABLE_CRON=false
CORS_ORIGINS=""

# Endurecimiento opcional: fallar si DATABASE_URL no parece de test
STRICT_TEST_DB=false
